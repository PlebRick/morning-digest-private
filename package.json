{
  "name": "morning-digest",
  "version": "1.0.0",
  "private": true,
  "description": "Automated daily news digest blog",
  "scripts": {
    "fetch": "node pipeline/fetch-feeds.js",
    "generate": "bash pipeline/generate-digest.sh",
    "serve": "node server.js",
    "build": "cd site && hugo --minify"
  },
  "dependencies": {
    "express": "^4.21.0",
    "rss-parser": "^3.13.0"
  }
}
